{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "Proxy server to relay external requests to the Forge backend API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nabihah Chaudhry",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
